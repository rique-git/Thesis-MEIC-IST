link: https://www.kaggle.com/datasets/fedesoriano/heart-failure-prediction

Observations:

The data is not clean, it has no missing values but there are some inconsistent values. There are few inconsistent values so we can treat them as outliers and remove them. Otherwise the data seems reliable.